<div data-index-icon>
    <x-tooltip id="{{ $id }}" placement="{{ $position }}" message="{{ $text }}">
        <span class="material-icons{{ $iconType }} text-purple text-sm ltr:ml-2 rtl:mr-2">
            {{ $icon }}
        </span>
    </x-tooltip>
</div>
