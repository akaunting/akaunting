# engines in package.json documents the Node range the asset pipeline builds on.
# Keep it advisory: a mismatch must warn, never block an install, so a stricter
# user or CI setting cannot break existing setups or app builds.
engine-strict=false
