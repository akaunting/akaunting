name: CI/CD Pipeline

on:
  push:
    branches:
      - master # You can specify the branch you want to trigger the workflow on

jobs:
  build:
    runs-on: ubuntu-latest # Specify the runner environment
    
    steps:
    - name: Verification
      run: echo "Verification Success" # This action verifies that ci.yml is working

    - name: Checkout Code
      uses: myrrym/group-akaunting # This action checks out the repository's code

    # - name: Build Application
    #   run: npm run build # Replace with your actual build command
      
    # - name: Run Tests
    #   run: npm test # Replace with your actual test command
